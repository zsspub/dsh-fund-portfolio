/** Model-facing tools persist canonical JSON results through the ordinary DSH tool pipeline. */
import type { Context } from '@deepseek-ai/cordis';
export declare const name = "fund-portfolio-tools";
export declare const inject: string[];
export declare function apply(ctx: Context): void;
